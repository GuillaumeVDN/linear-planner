server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location /linear-planner/ {
        alias /usr/share/nginx/html/linear-planner/;
        try_files $uri $uri/ /linear-planner/index.html;
    }

    location = / {
        return 301 /linear-planner/;
    }
}
